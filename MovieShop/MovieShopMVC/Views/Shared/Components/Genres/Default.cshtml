@model IEnumerable<ApplicationCore.Models.GenreModel>;

@foreach (var genre in Model){
     <a asp-controller="Genre" asp-action="List" asp-route-id="@genre.Id" class="dropdown-item"> @genre.Name </a>
}