@model IEnumerable<ApplicationCore.Models.GenreModel>;

<div class="list-group">
     @foreach (var genre in Model)
     {
          <a asp-controller="Genre" asp-action="List" asp-route-id="@genre.Id" class="list-group-item list-group-item-action">
               @genre.Name
          </a>
     }
</div>